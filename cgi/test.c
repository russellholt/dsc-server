#include <stdio.h>#include <stdlib.h>#include "entry.h"#ifndef debug#define debug 0#endif/* DoCGIThing: the 'main' routine; called from cgimain.c */void DoCGIThing(entries, m)entry entries[];int m;	/* max */{int x;char *cl;FILE *fp;char syscl[120];	printf("<title>This is just a test</title>");	printf("<center><H1>Just a Test and Stuff</H1><HR></center>");	puts("<p>The name-value pairs are:<p>");	for(x=0; x<=m; x++)		printf("<b>%s</b>: %s<p>\n", entries[x].name, entries[x].val);	printf("</pre>");	puts("<p><a href=\"/dest.html\">Destiny Software</a><p>");}